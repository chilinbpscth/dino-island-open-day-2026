# 後台實測跟進

負載測試清理：已逐一核對並清除兩批共 20 條測試探險者；真實讀榜確認只剩原本供老師覆核的「測試2806」。12 份已登記合成證書已設到期，保留資料行等既有排程刪檔。詳見 `load-test-cleanup.json`；之後不要重用已清理批次作恢復測試。

前端補充：已修正成績同步重入時提早返回的問題。證書頁會等最新排隊成績獲確認先上傳；錯誤／過舊回覆唔會清除本機佇列。`tests/sync-queue.mjs` 先重現錯誤，修正後連同證書先後次序測試通過；跨來源橋接、49 項單元測試及十科瀏覽器流程亦通過。此修正不等於 Google 十連線負載達標。測試負載頁的上傳等候上限亦已對齊正式證書頁的 60 秒，10 秒驗收目標不變；舊測試 30 秒逾時限制已在紀錄補註。

最新（17:04 HKT）：正式後台已更新至 v6，工作表物件重用及減少 metadata 呼叫已部署。新批次 `6918ac20-b787-4b67-b22b-28474584005a` 仍未達標：9 份成績確認、5 份證書完成核對；7 份成績於 10 秒內上榜、0 份證書於 10 秒內收到成功回覆。詳見 `live-load-v6-results.json`。伺服器紀錄已定位至少一項錯誤在證書預留資料行時等待共同鎖；不是只靠延長等待就能證明速度达標。下一步應減少成績及證書互相阻塞，並核對逾時後的實際資料，再作新測試。原已刪除證書請求重試亦已實測被拒絕。

2026-09-10：十連線實測未通過，詳見 `live-load-test-results.json`。上傳成功回覆不能當作下載成功；本次只有 2／10 完整驗證。

已加入 SpreadsheetApp.flush()，確保在釋放共同鎖前提交試算表寫入；48 項單元測試通過。2026-09-10 16:54 HKT 已透過 ITSUPPORT 將同一正式部署更新至 v5；執行身分及存取權限不變。先以同一批次作失敗恢復驗證，再作新批次速度測試；不能將部署成功視為現場驗收通過。

v5 恢復測試：6 份證書下載核對成功，4 個成績提交收到繁忙錯誤；未通過。原有分數已在榜上，不作新完成上榜速度證據。本機另減少重複開啟試算表及讀取欄數／列數的呼叫，只重用工作表物件、不快取資料行；49 項測試通過，但此優化未部署。

原到期測試證書 `cert-test-ca69ab18-8c1f-47d1-a4b2-26a2ddc19660`：Chrome ITSUPPORT 試算表讀回 `Certificates!H2` 為 `deleted`；原有排程最近執行時間 16:46:58 HKT、錯誤率 0%；精確 Drive 檔案網址顯示檔案不存在。沒有人工修改 H2。尚須驗證同一已刪除請求重試被拒絕。

保留負載測試批次 `e4512bfb-b92c-472a-aa1f-fbaa253cb8d5` 的精確參加者及證書 ID，待恢復驗證後清理測試資料；不要刪除其他參加者。實機、手機免登入儲存、老師內容覆核仍待完成。
