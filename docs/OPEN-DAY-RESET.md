# 2026-09-12 開放日前重置

用戶已確認現在重置。17:27 香港時間，後台成功備份並清空 68 筆測試玩家紀錄。

## 雲端結果

- 原試算表保留 `Players_Backup_20260912` 與鎖內建立的 `Players_PreReset_1789205245592`，可還原。
- `Players` 清空資料列，保留欄名；`DeviceSetup`、裝置憑證、`Certificates` 及私人 Drive 證書未更改。
- Apps Script 正式部署更新至 v11，沿用原 exec 網址、ITSUPPORT 執行身分及既有存取設定。
- `GAME_EPOCH=open-day-20260913`：舊客戶端與舊待傳紀錄沒有此標記，後台確認丟棄，不會重建玩家列。
- 編輯器臨時操作函式已移除；重置 helper 以底線結尾且不在 dispatch 白名單。重複執行相同 epoch 不會再清成績。

## 25 部 iPad

已連接的 iPad 用 Safari 開：
https://chilinbpscth.github.io/dino-island-open-day-2026/setup/open-day.html

入口下載及啟用新版後自動進入遊戲。首次需保持上網，素材約 115MB；等到歡迎頁再開始。關閉其他舊恐龍島遊戲分頁，避免繼續在舊介面操作。

新版使用獨立的正式進度空間，保留舊學號及連線設定。舊本機測試進度／照片保留在舊空間，不顯示、不加入新版同步佇列。這不是遠端抹除 25 部裝置，也不宣稱離線裝置已更新。

此入口不是「每次重置」按鈕；之後再開會保留正式來賓的進度。換小朋友仍用「下一位探險家」。未設定過的設備先依原 Classroom 學號指引設定。

## 驗證

62 個單元測試通過，包括舊紀錄補傳被拒、新 epoch 同裝置可寫入、裝置設定跨進度空間保留。GitHub Pages 部署工作成功。正式榜讀到空榜及 0 位探險中，更新時間 17:27:55。

Chrome 正式入口已實測自動進入空白歡迎頁，顯示「本機遊戲版本已可離線使用」。166 個正式素材網址全部回應 HTTP 200。

25 部實機需要各自開正式入口才完成本機更新；未聲稱已代為操作全部實機。
