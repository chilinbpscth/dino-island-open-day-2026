# 交付驗證 — 2026-09-07

## 本機已完成

- 靜態 Hub、11 個恐龍園區及分岔彎路地圖、11 科遊戲、計時及首次完成規則、六站領證、普通及拍照證書、下一位清理、老師設定頁、電視榜。
- A 校園繪本小蓮小志：14 個動作 PNG；兩人正側背及表情參考板。唯一小龍 3 個動作、角色板、3 個地標、島圖、證書框、榜裝飾。
- 學校提供 logo 原圖已套入；沒有 AI 重畫校徽。
- Apps Script / Sheets / 私人 Drive 後台原始碼、部署及現場操作說明。
- ITSUPPORT Apps Script 已建立並完成 `setup`：實際 Google Sheet 有 `Players`、`Certificates`、`DeviceSetup`，10 組裝置憑證齊全；私人證書資料夾及 `cleanupExpired_` 時間觸發器已核對。
- 44 項離線快取資源（含頁面入口）；預載後可斷網重開。

## 已通過的驗證及範圍

1. Node 13 項測試：暱稱、10 分計分、計時、筆順路徑及 Google 後台邏輯。Google 服務以 mock 模擬。
2. Chromium 全流程：以實際按鈕、描字指標、逐粒餵食、塗色及鼓完成任選 10 站；驗證每關 1 分、六分門檻、10 分滿分、第 11 關鎖定、完成站禁止重玩、假鏡頭拍照、換人清理及電話版面。零瀏覽器執行錯誤。體育倒數及長按曾在同一套遊戲流程驗證，規則本輪沒有修改。
3. Chromium 模擬離線：/school/ 子目錄、重開地圖、11 站及圖片快取、離線完成資訊並保留待傳佇列、普通證書生成。證書測試另五站資格以測試資料預設；十關操作另由全流程測試驗證。
4. 跨來源橋接 mock：模擬 Google 巢狀 iframe、驗證確認回應；舊請求回應不移除較新的待傳進度。
5. 1920×1080 合成榜資料：Top 10、8 字暱稱、站點及剩餘人數。不是現場距離測試。

原始結果：browser-test-results.json、offline-test-results.json、network-test-results.json；screenshots/ 為測試畫面，榜名字為合成資料、拍照使用測試鏡頭。

## 尚未完成，正式活動前必須處理

- 正式 GitHub Pages 網址及跨 iPad 現場接駁；Apps Script Web App v2、正式 `/exec` 網址及本機前端接駁已完成。
- 正式 HTTPS / GitHub Pages 網址；原始碼已有私人 GitHub repository，但尚未公開或啟用正式 Pages。
- 真實 iPad Safari 掃碼下載、到期後拒絕下載及排程自動刪除實測。桌面 Chromium 已驗證正式 Google 上載、免登入下載及七日到期時間；仍不能代替實體 iPad／手機測試。
- 10 部實體 iPad 同時成績及相片上傳；95% 10 秒內更新或出碼是待測目標，尚無實測數據。
- iPad Safari 相機、拒絕權限、觸控與離線持久性；iPhone Safari / Android Chrome 儲存證書；電視 3–5 米閱讀。
- 英文及普通話六段校方批准录音未提供，現時用視覺提示，音樂有本機鼓聲。
- 中文字形及筆順須老師簽收；遊戲幾何描線測試不等同官方字形認證。
- A 角色及校服最終視覺簽收，胸前徽章仍留空。現有素材為 PNG；沒有可編輯向量或綁骨架母稿。

所有來賓本機試玩毋須登入；未成功上傳不會生成假的 QR 下載碼。下一步部署方式見 DEPLOYMENT.md。

## 正式 Google 後台實測

2026-09-07 以 ITSUPPORT 正式 Web App v2 測試：匿名瀏覽器毋須 Google 登入即可開證書頁；本機來源成功讀取空榜。iPad 1 測試憑證先提交 9 站，沒有進入排行榜；版本 2 加入第 10 個不同站後，後台回傳 10 分、總有效時間 55,000ms、可領證，電視榜於下一輪查詢顯示 10／10 分及 00:55。另以 6 站測試紀錄上載 JPEG 到私人 Drive，後台回傳七日有效 token；另一個免登入瀏覽器成功顯示圖片、到期時間及儲存按鈕。所有測試紀錄及私人證書檔案其後已清除，榜回復空白。呢個係單一桌面 Chromium 真實 Google 測試，未取代十部實體 iPad 負載測試。

## 正式網站實測

2026-09-07 已由公開 `main` 分支啟用 GitHub Pages：`https://chilinbpscth.github.io/dino-island-open-day-2026/`。根網址正確轉到 `/hub/`；地圖示範顯示 11 個園區；`/setup/` 預填正式 Apps Script URL；HTTPS 及專案子目錄 service worker scope 正確。Apps Script `ALLOWED_ORIGINS` 已加入 `https://chilinbpscth.github.io`，以公開設定頁實測可讀取真實空榜；公開 `/board/` 完成 5 秒查詢並顯示最近更新時間。

## 最新規則修訂驗證

最新規則為 11 格／11 科任選 10 科、每關 1 分、6 分證書、10 分滿分後按總有效時間排名。新增後台 9 分不入榜、10 分入榜、前端滿分鎖定未選關卡及直接網址攔截測試。兩個 demo 入口亦驗證：可直接見地圖並點入科目，榜有 10 行示範資料；獨立儲存且不寫入正式進度／榜。

## 恐龍園區地圖修訂驗證

地圖已改為 11 個不同恐龍園區，分岔彎路供自由選擇。標題區顯示「佛教志蓮小學」、「智取恐龍島」及小蓮、小志兩人。Chromium 在 1024×768、1194×834 檢查全部 11 個熱區可見、至少 64px、中心點正確進入科目；任意次序、完成鎖定、六站領證及手機橫向捲動均通過。詳見 park-map-test-results.json 及 screenshots/park-map-*.png。更新後已重新建立 44 項快取資源，離線重開、11 站資源及普通證書驗證通過；未取代實體 iPad 測試。

導遊互動修訂：兩位角色在地圖道路上移動，到達後才開關。已驗證延遲進關、連撳保護、任意園區次序、返回原位置、減少動態、途中離開取消及無執行錯誤。

完成狀態修訂：完成園區改用金色科目牌、牌內深綠「✓ 完成 · 1 分」橫章及固定圓形剔號；10 個完成標記與 1 個「今次未選」標記在 1194×834 畫面均完整顯示，文字沒有裁切。
